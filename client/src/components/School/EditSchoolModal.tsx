import React, { ReactElement } from 'react'
import {
    Modal,
    ModalOverlay,
    ModalContent,
    ModalHeader,
    ModalFooter,
    ModalBody,
    ModalCloseButton,
} from '@chakra-ui/react';

interface Props {
    
}

export default function EditSchoolModal({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
